import { LineaDetalle } from '../lib/entity/linea-detalle.entity';
import { OrdenCompra } from '../lib/entity/orden-compra.entity';

// describe('Borrado masivo de ordenes', () => {
//   it('Borrado masivo de ordenes por id', () => {
//     const orden:OrdenCompra = crearOrdenPrueba();
//     expect(findOrden(orden.id)).toBeTruthy()
//     borrarOrdenes([orden]);
//     expect(findOrden(orden.id)).toBeFalsy()
//   });
// });
// function crearOrdenPrueba():OrdenCompra {
//     const o = new OrdenCompra();
//     const l = new LineaDetalle();

//     o.lineas = [linea]
// }
